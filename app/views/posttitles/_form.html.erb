<%= form_for(@posttitle) do |f| %>
  <% if @posttitle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@posttitle.errors.count, "error") %> prohibited this posttitle from being saved:</h2>

      <ul>
      <% @posttitle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
